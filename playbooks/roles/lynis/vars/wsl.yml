---
lynis_skip_tests:
  # Couldn't find 2 responsive nameservers [NETW-2705]
  # /etc/resolv.conf is automatically generated by WSL
  - NETW-2705

  # klogd is not running, which could lead to missing kernel messages in log
  # files [LOGG-2138]
  - LOGG-2138

  # Add a legal banner to /etc/issue, to warn unauthorized users [BANN-7126]
  - BANN-7126

  # Add legal banner to /etc/issue.net, to warn unauthorized users [BANN-7130]
  - BANN-7130

  # Could not find any kernel packages via package manager
  - KRNL-5840:1

  # Determine if system uses LUKS block device encryption
  - CRYP-7930

  # Determine if system uses encrypted swap
  - CRYP-7931

  # AFICK availability
  - FINT-4310

  # AIDE availability
  - FINT-4314

  # Check AIDE configuration file
  - FINT-4315

  # Presence of AIDE database and size check
  - FINT-4316

  # Osiris availability
  - FINT-4318

  # Samhain availability
  - FINT-4322

  # Tripwire availability
  - FINT-4326

  # OSSEC syscheck daemon running
  - FINT-4328

  # Check for presence of Snort
  - TOOL-5120

  # Check Snort configuration file
  - TOOL-5122

  # Check for active OSSEC daemon
  - TOOL-5126

  # Check presence of IDS/IPS tool
  - TOOL-5190

  # Harden compilers like restricting access to root user only [HRDN-7222]
  # This is a development system.
  - HRDN-7222

  # Not using separate partitions on WSL environments
  - FILE-6310

  # When possible set expire dates for all password protected accounts
  # [AUTH-9282]
  - AUTH-9282

  # Configure minimum password age in /etc/login.defs [AUTH-9286]
  - AUTH-9286

  # Configure maximum password age in /etc/login.defs [AUTH-9286]
  - AUTH-9286

  # iptables module(s) loaded, but no rules active [FIRE-4512]
  - FIRE-4512

  # Reboot of system is most likely needed [KRNL-5830]
  - KRNL-5830

  # Check for running NTP daemon or client
  - TIME-3104

  # One or more sysctl values differ from the scan profile and could be tweaked
  # [KRNL-6000]
  - KRNL-6000:fs.protected_hardlinks
  - KRNL-6000:fs.protected_symlinks
  - KRNL-6000:kernel.core_uses_pid
  - KRNL-6000:kernel.ctrl-alt-del
  - KRNL-6000:kernel.dmesg_restrict
  - KRNL-6000:kernel.kptr_restrict
  - KRNL-6000:net.ipv4.conf.all.accept_redirects
  - KRNL-6000:net.ipv4.conf.all.log_martians
  - KRNL-6000:net.ipv4.conf.all.rp_filter
  - KRNL-6000:net.ipv4.conf.all.send_redirects
  - KRNL-6000:net.ipv4.conf.default.accept_redirects
  - KRNL-6000:net.ipv4.conf.default.accept_source_route
  - KRNL-6000:net.ipv4.conf.default.log_martians
  - KRNL-6000:net.ipv6.conf.all.accept_redirects
  - KRNL-6000:net.ipv6.conf.default.accept_redirects
