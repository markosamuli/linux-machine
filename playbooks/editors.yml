---

- name: Editors
  hosts: all
  tags:
  - editors

  tasks:

  - include: tasks/vscode.yml
    tags:
    - vscode
    - frontend

  - name: Install vim
    package:
      name: vim
    tags:
    - vim
